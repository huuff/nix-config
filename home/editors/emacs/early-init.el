;; prevents a flashing white background while emacs is initializing
(setq default-frame-alist '((background-color . "#282C34")
                            (ns-appearance . dark)
                            (ns-transparent-titlebar . t)))

