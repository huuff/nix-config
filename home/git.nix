{ pkgs, ... }: {

  programs.git = {
    enable = true;
    userName = "Haf";
    userEmail = "haf@protonmail.ch";
    ignores = [ 
      "*~"
      "*.swp" # vim swap files 
      "texfrag" # trash generated by textfrag in emacs
      "\\#*\\#" # emacs swap files
      "result" # nix outputs
      "result-data" # something related to nix output
    ];
    extraConfig = {
      credential.helper = "cache --timeout=3600";
      init.defaultBranch = "master";
      pull.rebase = "false";
    };
  };

}
